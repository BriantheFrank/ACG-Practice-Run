# Optional. Without this key the app falls back to a local conservative graph/OPORD builder.
OPENAI_API_KEY=
# Optional for analysis generation, required to persist drafts and version history.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
